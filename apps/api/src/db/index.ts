import { connectDatabase, checkDatabaseHealth, closeDatabase } from './client';
import { bootstrapIndexes } from './indexes';

export { connectDatabase, closeDatabase, checkDatabaseHealth, bootstrapIndexes };
